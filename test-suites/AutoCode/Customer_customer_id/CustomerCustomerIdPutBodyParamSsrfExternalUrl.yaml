---
parent: Customer_customer_id
name: CustomerCustomerIdPutBodyParamSsrfExternalUrl
description: null
authors:
- apisec Bot
type: SUITE
init: []
localVariables: []
path: /customer/{customer_id}
endpoint: /customer/{{@Var.customer_id}}
method: PUT
auth: Default
headers:
- 'Accept: application/json'
- 'Content-Type: application/json'
testCases:
- id: 1
  body: |-
    {
      "name" : "{{@Injection}}",
      "customer_realname" : "{{@Injection}}",
      "is_active" : false,
      "active_from" : "{{@Var.active_from}}",
      "active_until" : "{{@Var.active_until}}",
      "max_users" : "{{@Var.max_users}}",
      "contact_name" : "{{@Injection}}",
      "contact_email" : "{{@Var.contact_email}}",
      "comment" : "{{@Injection}}",
      "available_bytes" : "{{@Var.available_bytes}}",
      "is_freezed" : "{{@Var.is_freezed}}",
      "freezed_message" : "{{@Injection}}",
      "modules" : [ "{{@Random}}" ],
      "send_admin_email" : false,
      "admin_email" : "{{@Var.admin_email}}",
      "admin_loginname" : "{{@Injection}}",
      "admin_firstname" : "{{@Var.admin_firstname}}",
      "admin_lastname" : "{{@Var.admin_lastname}}"
    }
  inactive: false
useCategoryAssertion: false
assertions:
- '@Response !=~ (?s:.)*<(?s:.*)(APISec External SSRF)(?s:.*)>(?s:.*)'
- '@ResponseTime < 7000 OR @StatusCode == 400'
cleanup: []
severity: High
category: ssrf_external_url
tags:
- V1
- OWASP-API8
- OWASP API Security Top 10
- SSRF
inactive: false
policies:
  initExec: Request
  cleanupExec: Request
  logger: DEBUG
  timeoutSeconds: 300
  repeatOnFailure: 0
  repeat: 0
  repeatModule: '@APIsec/Common/ssrf_external_url'
  repeatDelay: 0
  unmaskToken: false
  postExecutionDelay: 0
  showResponse: true
autoGenerated: true
